name: Publish Release

on:
  pull_request:
    types: [closed]
    branches:
      - master
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version without v prefix (example: 0.0.3)'
        required: true
        type: string

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    name: Publish to npm
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true &&
       contains(github.event.pull_request.labels.*.name, 'area:release'))
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org/

      - name: Pin npm
        run: npm install -g npm@11.17.0

      - name: Resolve version
        id: meta
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="$(node -p "require('./packages/cli/package.json').version")"
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Verify package version
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -euo pipefail
          CLI="$(node -p "require('./packages/cli/package.json').version")"
          MCP="$(node -p "require('./packages/mcp/package.json').version")"
          CLI_DEP="$(node -p "require('./packages/mcp/package.json').dependencies['@ng-elemental/cli']")"

          if [ "$CLI" != "$VERSION" ]; then
            echo "packages/cli/package.json is $CLI, expected $VERSION"
            exit 1
          fi
          if [ "$MCP" != "$VERSION" ]; then
            echo "packages/mcp/package.json is $MCP, expected $VERSION"
            exit 1
          fi
          if [ "$CLI_DEP" != "$VERSION" ]; then
            echo "packages/mcp depends on @ng-elemental/cli $CLI_DEP, expected $VERSION"
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      - name: Build CLI and MCP
        run: npx nx run-many -t build --projects=cli,mcp

      - name: Verify publish output
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -euo pipefail

          test -f dist/packages/cli/bin.cjs
          test -f dist/packages/cli/index.cjs
          test -f dist/packages/cli/README.md
          test -d dist/packages/cli/registry
          test -f dist/packages/cli/registry/button/button.ts

          test -f dist/packages/mcp/bin.cjs
          test -f dist/packages/mcp/index.cjs
          test -f dist/packages/mcp/README.md
          test -f dist/packages/mcp/guidelines.md
          test -d dist/packages/mcp/ui-source
          test -f dist/packages/mcp/ui-source/button/button.ts

          CLI_NAME="$(node -p "require('./dist/packages/cli/package.json').name")"
          CLI_VER="$(node -p "require('./dist/packages/cli/package.json').version")"
          CLI_BIN="$(node -p "require('./dist/packages/cli/package.json').bin['ng-elemental']")"
          MCP_NAME="$(node -p "require('./dist/packages/mcp/package.json').name")"
          MCP_VER="$(node -p "require('./dist/packages/mcp/package.json').version")"
          MCP_BIN="$(node -p "require('./dist/packages/mcp/package.json').bin['ng-elemental-mcp']")"
          MCP_CLI_DEP="$(node -p "require('./dist/packages/mcp/package.json').dependencies['@ng-elemental/cli']")"

          [ "$CLI_NAME" = "@ng-elemental/cli" ]
          [ "$CLI_VER" = "$VERSION" ]
          [ "$CLI_BIN" = "./bin.cjs" ]
          [ "$MCP_NAME" = "@ng-elemental/mcp" ]
          [ "$MCP_VER" = "$VERSION" ]
          [ "$MCP_BIN" = "./bin.cjs" ]
          [ "$MCP_CLI_DEP" = "$VERSION" ]

          node dist/packages/cli/bin.cjs --help | grep -q "@ng-elemental/cli"

      - name: Test packed CLI and MCP
        run: npx nx test mcp

      - name: Publish @ng-elemental/cli
        id: publish
        working-directory: dist/packages/cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          PUBLISHED="$(npm view @ng-elemental/cli version 2>/dev/null || true)"

          if [ "$VERSION" = "$PUBLISHED" ]; then
            echo "Version $VERSION is already on npm. Skipping publish."
            echo "skipped=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          npm publish --access public --provenance
          echo "skipped=false" >> "$GITHUB_OUTPUT"

      - name: Publish @ng-elemental/mcp
        working-directory: dist/packages/mcp
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          VERSION="${{ steps.meta.outputs.version }}"
          PUBLISHED="$(npm view @ng-elemental/mcp version 2>/dev/null || true)"

          if [ "$VERSION" = "$PUBLISHED" ]; then
            echo "Version $VERSION is already on npm. Skipping MCP publish."
            exit 0
          fi

          npm publish --access public --provenance

      - name: Update release tag
        env:
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$TAG"
          git push -f origin "$TAG"

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.meta.outputs.tag }}
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -euo pipefail

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "GitHub Release $TAG already exists."
            exit 0
          fi

          NOTES="$(awk -v ver="$VERSION" '
            $0 ~ "^## \\[" ver "\\]" { capture = 1; next }
            /^## \\[/ && capture { exit }
            capture { print }
          ' CHANGELOG.md)"
          NOTES="$(printf '%s\n' "$NOTES" | sed '/./,$!d')"

          if [ -z "$NOTES" ]; then
            NOTES="Release ${TAG}"
          fi

          gh release create "$TAG" \
            --title "$TAG" \
            --notes "$NOTES" \
            --verify-tag
