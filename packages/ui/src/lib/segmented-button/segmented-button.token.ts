import { InjectionToken } from '@angular/core';
import type { ElSegmentedButton } from './segmented-button';

export const EL_SEGMENTED_BUTTON = new InjectionToken<ElSegmentedButton>(
  'ElSegmentedButton',
);
